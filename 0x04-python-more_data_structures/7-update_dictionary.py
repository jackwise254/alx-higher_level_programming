#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    # Update the value for the given key or add a new key-value pair if the key doesn't exist
    a_dictionary[key] = value
    return a_dictionary
